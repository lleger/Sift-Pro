<%= simple_form_for @university, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> University</legend>

    <%= f.input :name %>
    <%= f.input :location %>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', universities_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
